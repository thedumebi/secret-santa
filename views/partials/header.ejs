<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <!-- <meta name="viewport" content="width=device-width"/> -->
    <meta
      name="viewport"
      content="width=device-width, initial-scale=0.8, user-scalable=no, maximum-scale=1, mimimum-scale=1"
    />
    <title>Secret Santa</title>
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
    />
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.8.1/css/v4-shims.css"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>

  <body>
    <div class="backdrop"></div>

    <div class="modal">
      <h1 class="modal__title">Are you sure you want to logout?</h1>
      <div class="modal__actions">
        <form action="/logout" method="POST">
          <button type="submit" class="modal__action btn">Yes!</button>
        </form>
        <button type="submit" class="modal__action modal__action--negative btn">
          No!
        </button>
      </div>
    </div>

    <header class="main-header">
      <div>
        <button class="toggle-button">
          <span class="toggle-button__bar"></span
          ><span class="toggle-button__bar"></span
          ><span class="toggle-button__bar"></span>
        </button>

        <a href="/" class="main-header__brand">
          <img src="/images/uhost-icon.png" alt="Secret Santa" />
        </a>
      </div>

      <nav class="main-nav">
        <ul class="main-nav__items">
          <% if (locals.user) { %> <% if (user.is_admin) { %>
          <li class="main-nav__item">
            <a href="/users">Users</a>
          </li>
          <li class="main-nav__item">
            <a href="/users/unpaired">Unpaired Users</a>
          </li>
          <% } %>
          <li class="main-nav__item">
            <a href="/user/<%= user._id %>">Profile</a>
          </li>
          <li class="main-nav__item logout">
            <a href="#">Logout</a>
          </li>
          <% } else { %>
          <li class="main-nav__item">
            <a href="/register">Register</a>
          </li>
          <li class="main-nav__item main-nav__item--cta">
            <a href="/login">Login</a>
          </li>
          <% } %>
        </ul>
      </nav>
    </header>

    <nav class="mobile-nav">
      <ul class="mobile-nav__items">
        <% if (locals.user) { %> <% if (user.is_admin) { %>
        <li class="mobile-nav__item">
          <a href="/users">Users</a>
        </li>
        <li class="mobile-nav__item">
          <a href="/users/unpaired">Unpaired Users</a>
        </li>
        <% } %>
        <li class="mobile-nav__item">
          <a href="/user/<%= user._id %>">Profile</a>
        </li>
        <li class="mobile-nav__item logout">
          <a href="#">Logout</a>
        </li>
        <% } else { %>
        <li class="mobile-nav__item">
          <a href="/register">Register</a>
        </li>
        <li class="mobile-nav__item mobile-nav__item--cta">
          <a href="/login">Login</a>
        </li>
        <% } %>
      </ul>
    </nav>

    <main>
